Para instalar Ansible

#Ubuntu
sudo apt upate -y
sudo apt-get install ansible

# Ubuntu
apt update
apt install software-properties-common
apt-add-repository --yes --update ppa:ansible/ansible
apt install -y ansible 

# Amazon linux 2
sudo yum update -y
sudo amazon-linux-extras install ansible2 -y

Ejecutar un ansible playbook

- sudo ansible-playbook -i <INVENTORY_TO_USE> <DIR_TO_YOUR_MAIN_YML_FILE>
- ansible-playbook -vvv -i <INVENTORY_TO_USE> -l <TARGET> <DIRECTORY_MASTER_PLAYBOOK> -e <EXTRAS>